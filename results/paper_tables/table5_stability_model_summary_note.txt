Caption note: Stability summary is model-wise mean over datasets in results/tree_vs_deep_stability_analysis/summary_metrics.csv (drybean, telco, titanic). Delta columns are computed as shift - clean (negative means degradation under shift). SHAP Spearman/Jaccard means are averaged over available entries only; NA indicates unavailable values (e.g., models that failed in this environment or models without SHAP stability computation).
